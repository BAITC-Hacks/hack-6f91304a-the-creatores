"""ЗакупAI: Excel normalization, demand forecasts, and order calculations.

Use ``backend.calculations.provider`` for the existing HTTP backend contract.
Individual modules are independent of application startup and external services.
"""
